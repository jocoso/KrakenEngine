from setuptools import find_packages, setup

setup(
    name='kraken',
    packages=find_packages(include=['kk']),
    version='0.1.0',
    description="A text adventure library",
    author='Jocoso',
    license='MIT',
)